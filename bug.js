```javascript
function MyComponent() {
  const [count, setCount] = useState(0);

  useEffect(() => {
    // Incorrect logic: This will cause an infinite loop
    setCount(count + 1);
  }, [count]); // Dependency on count leads to infinite rerenders

  return <div>Count: {count}</div>;
}
```